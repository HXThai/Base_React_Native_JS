import * as types from '../actionstypes';

const initReducer = {
  AccessToken: '90099-45645-34563456-456456-456456',
};
const homeReducers = (state = initReducer, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default homeReducers;
